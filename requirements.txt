# Core framework. [launch] extra pulls in hydra-core, mlflow, termcolor, etc.
nvidia-physicsnemo[launch]==1.3.0

# Install torch matching your hardware BEFORE this file, per
# https://pytorch.org/get-started/locally/ -- e.g. for CPU-only (what this
# project was locally validated with, no GPU available in that environment):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# On a CUDA training box (e.g. an H100 VM), install a matching CUDA build
# instead, before this file, e.g. for CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0

# physicsnemo.datapipes.cae unconditionally imports NVIDIA DALI at package
# import time (see README "Environment notes" -- this is required even
# though DoMINO's .zarr path here never calls it). On a real CUDA machine,
# install the real package instead of using devtools/dali_stub -- pick the
# line matching your CUDA major version (uncomment one):
#   nvidia-dali-cuda120>=1.35.0   # CUDA 12.x (H100 VMs are almost always this)
#   nvidia-dali-cuda110>=1.35.0   # CUDA 11.x
# Not pip-installable on macOS/no-GPU machines -- that's what the stub is for.

# physicsnemo 1.3.0's neighbor/radius_search and SDF code (used by DoMINO's
# datapipe) breaks on warp-lang >= 1.8 (`AttributeError: module 'warp' has no
# attribute 'context'` at import time). Pin to a version verified to work.
warp-lang==1.7.2

# physicsnemo.datapipes.cae imports these unconditionally at package import
# time (even though the DoMINO/.zarr code path here doesn't use vtk/pyvista
# directly) -- see README "Environment notes".
vtk
pyvista

# Used by test.py for parity plots.
matplotlib

# torch.utils.tensorboard requires this even though it isn't a torch dep.
tensorboard

zarr>=2.14.2 # tested against zarr 3.3.0
hydra-core>=1.2.0
omegaconf
mlflow
torchinfo
tabulate

# Optional: per-step GPU memory logging in train.py. Skipped automatically
# (no error) if this isn't installed or no CUDA device is present.
pynvml
